---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "gateway-service"
  labels:
    app.kubernetes.io/name: "datarizen"
    app.kubernetes.io/component: "gateway"
    app.kubernetes.io/instance: "{{.Release.Name}}"
spec:
  type: "ClusterIP"
  selector:
    app.kubernetes.io/name: "datarizen"
    app.kubernetes.io/component: "gateway"
    app.kubernetes.io/instance: "{{.Release.Name}}"
  ports:
    - name: "http"
      protocol: "TCP"
      port: {{ .Values.parameters.gateway.port_http | int }}
      targetPort: {{ .Values.parameters.gateway.port_http | int }}
