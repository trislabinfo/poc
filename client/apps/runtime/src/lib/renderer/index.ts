/**
 * Renderer boundary: consumes ApplicationSnapshot + config and renders layout/pages/components.
 * Entry point for the runtime renderer; main app imports from here only.
 */

export { default as RuntimeRenderer } from './RuntimeRenderer.svelte';
