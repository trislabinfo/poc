# Capabilities.FeatureFlags

Feature flag management abstractions.

## Structure

- `Services/`
  - `IFeatureFlagService`, `FeatureFlagService` – resolve and evaluate feature flags.

## Dependencies

- `BuildingBlocks.Kernel`

