# Capabilities.Authorization

Policy-based authorization helpers.

## Structure

- `Policies/`
  - `TenantPolicyHandler`, `PermissionPolicyHandler` – custom policy handlers.
- `Requirements/`
  - `TenantRequirement`, `PermissionRequirement` – authorization requirements.

## Dependencies

- `BuildingBlocks.Kernel`

